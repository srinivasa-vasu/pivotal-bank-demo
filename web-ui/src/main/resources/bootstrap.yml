spring:
  application:
     name: web-ui-service
  cloud:
    config:
      uri: http://config-service
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}
