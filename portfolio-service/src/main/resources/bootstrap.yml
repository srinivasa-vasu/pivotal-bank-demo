spring:
#  profiles.active: local
  application:
     name: portfolio-service
  cloud:
    config:
      uri: http://config-service
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}
